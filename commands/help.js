var helpCommand = {
    usage: "",
    description: "Displays a helpful message about commands and stuff",
    process: function (bot, msg, suffix) {
        //TODO: Print all commands with desc and stuff
    }
};

module.exports = helpCommand;
